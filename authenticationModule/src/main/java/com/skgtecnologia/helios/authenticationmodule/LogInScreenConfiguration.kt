package com.skgtecnologia.helios.authenticationmodule

/**Configuration for place holders title,
 * @author Kevin Peñaranda
 * **/
/*
class LogInScreenConfiguration private constructor(
    val ScreenSubTitle: DeferredText,

) {
    class Builder {
        @set:JvmSynthetic
        var ScreenSubTitle: DeferredText = Companion.ScreenSubTitle



        fun build() =
            LogInScreenConfiguration(ScreenSubTitle,

            )

    }

    internal companion object {
        val ScreenSubTitle = DeferredText.Resource(R.string.app_customerData_subtitle_text)

    }
}

@Suppress("FunctionName")
@JvmSynthetic
fun LogInScreenConfiguration(initializer: LogInScreenConfiguration.Builder.() -> Unit): LogInScreenConfiguration =
    LogInScreenConfiguration.Builder().apply(initializer).build()*/
